/**
 * Official upstream brand snapshot.
 *
 * Re-exported from the shared product-config package so the upstream release
 * gate and the generic validator share one source of truth with the runtime
 * validator (fork-identity reference).
 */

export { OFFICIAL_BRAND } from "../../../packages/product-config/src/index.ts";
