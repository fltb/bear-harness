// Keep the package's runtime ESM separate from its source-level TypeScript
// declaration, which is compiled with a different noUncheckedIndexedAccess policy.
export { default } from "marked-katex-extension";
