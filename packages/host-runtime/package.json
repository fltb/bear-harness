{
	"name": "@bear-harness/host-runtime",
	"version": "1.0.0",
	"private": true,
	"type": "module",
	"exports": {
		".": {
			"types": "./dist/index.d.ts",
			"import": "./dist/index.js"
		}
	},
	"scripts": {
		"prebuild": "npm --prefix ../tdai-core run build && npm --prefix ../protocol run build",
		"build": "tsc -p tsconfig.json && node scripts/check-esm-runtime.mjs",
		"db:generate": "drizzle-kit generate",
		"pretypecheck": "npm --prefix ../tdai-core run build && npm --prefix ../protocol run build",
		"typecheck": "tsc -p tsconfig.json --noEmit",
		"test:unit": "npm --prefix ../tdai-core run build && npm --prefix ../protocol run build && vitest run",
		"test:release:recovery": "npm --prefix ../tdai-core run build && npm --prefix ../protocol run build && vitest run tests/durable-file-transaction.spec.ts tests/character-import.spec.ts tests/companion-state-store.spec.ts tests/session-catalog.spec.ts",
		"test:coverage": "npm --prefix ../tdai-core run build && npm --prefix ../protocol run build && vitest run --coverage"
	},
	"dependencies": {
		"@agentclientprotocol/codex-acp": "^1.2.0",
		"@agentclientprotocol/sdk": "^1.3.0",
		"@bear-harness/protocol": "file:../protocol",
		"@bear-harness/schema": "file:../schema",
		"@bear-harness/tdai-core": "file:../tdai-core",
		"@earendil-works/pi-agent-core": "^0.84.3",
		"@earendil-works/pi-ai": "^0.84.3",
		"@earendil-works/pi-coding-agent": "^0.84.3",
		"ajv": "^8.20.0",
		"drizzle-orm": "^1.0.0-rc.4",
		"fast-json-patch": "^3.1.1",
		"immer": "^10.1.3",
		"officeparser": "^7.8.0",
		"p-queue": "^8.1.0",
		"proper-lockfile": "^4.1.2",
		"sqlite-vec": "^0.1.7-alpha.2",
		"undici": "^7.0.0",
		"yaml": "2.9.0"
	},
	"devDependencies": {
		"@types/node": "24.13.3",
		"@types/proper-lockfile": "^4.1.4",
		"@vitest/coverage-v8": "4.1.10",
		"drizzle-kit": "^1.0.0-rc.4",
		"typescript": "7.0.2",
		"vitest": "4.1.10"
	}
}
