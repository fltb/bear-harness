name: bear-harness-ci
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  quality:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7.0.1
      - uses: actions/setup-node@v7.0.0
        with:
          node-version: 24.19.0
          cache: npm
      - run: npm ci
      - run: npm run lint
      - run: npm run typecheck
      - run: npm run test:coverage
      - uses: actions/upload-artifact@v7.0.1
        with:
          name: coverage
          path: coverage/
          if-no-files-found: error
      - run: npm run build
      - run: node scripts/release-attestation.mjs quality
      - uses: actions/upload-artifact@v7.0.1
        with:
          name: release-attestation-quality
          path: release-attestations/quality.json
          if-no-files-found: error

  upstream-brand:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7.0.1
      - uses: actions/setup-node@v7.0.0
        with:
          node-version: 24.19.0
          cache: npm
      - run: npm ci
      - run: node apps/desktop/scripts/check-upstream-brand.mjs

  security:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7.0.1
      - uses: actions/setup-node@v7.0.0
        with:
          node-version: 24.19.0
          cache: npm
      - run: npm ci
      - run: npm audit --audit-level=high
      - run: npm audit signatures

  recovery:
    needs: [quality]
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7.0.1
      - uses: actions/setup-node@v7.0.0
        with:
          node-version: 24.19.0
          cache: npm
      - run: npm ci
      - run: npm run test:release:recovery
      - run: node scripts/release-attestation.mjs recovery
      - uses: actions/upload-artifact@v7.0.1
        with:
          name: release-attestation-recovery
          path: release-attestations/recovery.json
          if-no-files-found: error

  e2e:
    needs: [quality]
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7.0.1
      - uses: actions/setup-node@v7.0.0
        with:
          node-version: 24.19.0
          cache: npm
      - run: npm ci
      - run: npm run build
      - name: Install Electron runtime libraries
        run: |
          sudo apt-get update
          sudo apt-get install -y libnss3 libatk1.0-0 libatk-bridge2.0-0 libcups2 libdrm2 libgtk-3-0 libgbm1 libasound2t64 libxss1 libxshmfence1 xvfb
      - run: xvfb-run -a npm run test:e2e:electron
      - run: node scripts/release-attestation.mjs electron-e2e
      - uses: actions/upload-artifact@v7.0.1
        with:
          name: release-attestation-electron-e2e
          path: release-attestations/electron-e2e.json
          if-no-files-found: error

  web-e2e:
    needs: [quality]
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7.0.1
      - uses: actions/setup-node@v7.0.0
        with:
          node-version: 24.19.0
          cache: npm
      - run: npm ci
      - run: npx --no-install playwright install --with-deps chromium
      - run: npm run test:e2e:web:required
      - run: node scripts/release-attestation.mjs web-e2e
      - uses: actions/upload-artifact@v7.0.1
        with:
          name: release-attestation-web-e2e
          path: release-attestations/web-e2e.json
          if-no-files-found: error

  live-model:
    needs: [quality]
    runs-on: ubuntu-24.04
    env:
      BEAR_E2E_LIVE_MODEL: "1"
      BEAR_E2E_PROVIDER_ID: ${{ secrets.BEAR_E2E_PROVIDER_ID }}
      BEAR_E2E_MODEL_ID: ${{ secrets.BEAR_E2E_MODEL_ID }}
      BEAR_E2E_API_KEY: ${{ secrets.BEAR_E2E_API_KEY }}
      BEAR_E2E_CUSTOM_BASE_URL: ${{ secrets.BEAR_E2E_CUSTOM_BASE_URL }}
    steps:
      - uses: actions/checkout@v7.0.1
      - uses: actions/setup-node@v7.0.0
        with:
          node-version: 24.19.0
          cache: npm
      - run: npm ci
      - run: npx --no-install playwright install --with-deps chromium
      - run: npm run test:e2e:web:live
      - run: node scripts/release-attestation.mjs live-model
      - uses: actions/upload-artifact@v7.0.1
        with:
          name: release-attestation-live-model
          path: release-attestations/live-model.json
          if-no-files-found: error

  package:
    needs: [quality, web-e2e, e2e]
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos-13
            os-name: mac
            arch: x64
            package-cmd: package:mac:x64
            exts: dmg zip
            upload-glob: |
              apps/desktop/release/*.dmg
              apps/desktop/release/*.zip
          - os: macos-15
            os-name: mac
            arch: arm64
            package-cmd: package:mac:arm64
            exts: dmg zip
            upload-glob: |
              apps/desktop/release/*.dmg
              apps/desktop/release/*.zip
          - os: windows-2025
            os-name: win
            arch: x64
            package-cmd: package:win
            exts: exe zip
            upload-glob: |
              apps/desktop/release/*.exe
              apps/desktop/release/*.zip
          - os: ubuntu-24.04
            os-name: linux
            arch: x64
            package-cmd: package:linux
            exts: AppImage deb
            upload-glob: |
              apps/desktop/release/*.AppImage
              apps/desktop/release/*.deb
    env:
      CSC_IDENTITY_AUTO_DISCOVERY: "false"
    steps:
      - uses: actions/checkout@v7.0.1
      - uses: actions/setup-node@v7.0.0
        with:
          node-version: 24.19.0
          cache: npm
      - run: npm ci
      - run: npm run build
      - name: Crashpad smoke (production config, temp root)
        run: npm run test:diagnostics:crash
      - name: Package
        run: npm run ${{ matrix.package-cmd }}
      - name: Verify dynamic artifact names and sizes
        run: node scripts/verify-package.mjs ${{ matrix.os-name }} ${{ matrix.arch }} ${{ matrix.exts }}
      - name: Verify macOS target architecture
        if: matrix.os-name == 'mac'
        shell: bash
        run: |
          set -euo pipefail
          APP=$(ls -d apps/desktop/release/mac*/*.app | head -1)
          EXE="$APP/Contents/MacOS/$(node --input-type=module -e "const { productConfig } = await import('@bear-harness/product-config'); process.stdout.write(productConfig.executableName)")"
          FRAMEWORK="$APP/Contents/Frameworks/Electron Framework.framework/Versions/A/Electron Framework"
          if [ "${{ matrix.arch }}" = "arm64" ]; then
            lipo -archs "$EXE" | grep -qx "arm64"
            lipo -archs "$FRAMEWORK" | grep -qx "arm64"
          else
            lipo -archs "$EXE" | grep -qx "x86_64"
            lipo -archs "$FRAMEWORK" | grep -qx "x86_64"
          fi
      - name: Verify native llama binding
        shell: bash
        run: node apps/desktop/scripts/verify-native-bindings.mjs ${{ matrix.os-name }} ${{ matrix.arch }}
      - name: Verify Windows x64 PE machine
        if: matrix.os-name == 'win'
        shell: pwsh
        run: |
          $exeName = node --input-type=module -e "const { productConfig } = await import('@bear-harness/product-config'); process.stdout.write(productConfig.executableName)"
          $exe = Get-ChildItem apps/desktop/release/win-unpacked -Filter "$exeName.exe" | Select-Object -First 1
          if (-not $exe) { throw "packaged exe not found" }
          node --input-type=module -e "const fs = require('node:fs'); const buf = fs.readFileSync(process.argv[1]); const pe = buf.readUInt32LE(0x3c); const machine = buf.readUInt16LE(pe + 4); if (machine !== 0x8664) { throw new Error('expected x64 (0x8664), got 0x' + machine.toString(16)); }" $exe.FullName
      - name: Verify bundled Windows Git runtime
        if: matrix.os-name == 'win'
        shell: pwsh
        run: node apps/desktop/scripts/verify-windows-runtime.mjs apps/desktop/release/win-unpacked/resources
      - name: Smoke bundled Windows Git executables
        if: matrix.os-name == 'win'
        shell: pwsh
        run: |
          & apps/desktop/release/win-unpacked/resources/git/usr/bin/bash.exe --version
          & apps/desktop/release/win-unpacked/resources/git/cmd/git.exe --version
      - name: Verify Linux x64 artifacts
        if: matrix.os-name == 'linux'
        shell: bash
        run: |
          set -euo pipefail
          DEB=$(ls apps/desktop/release/*.deb | head -1)
          dpkg-deb --info "$DEB" | grep -q "Architecture: amd64"
          APPIMAGE=$(ls apps/desktop/release/*.AppImage | head -1)
          chmod +x "$APPIMAGE"
          APPIMAGE_EXTRACT_AND_RUN=1 "$APPIMAGE" --appimage-extract >/dev/null
          BIN=$(node --input-type=module -e "const { productConfig } = await import('@bear-harness/product-config'); process.stdout.write(productConfig.executableName)")
          file "squashfs-root/usr/bin/$BIN" | grep -q "x86-64"
      - name: Packaged app smoke
        shell: bash
        env:
          APPIMAGE_EXTRACT_AND_RUN: "1"
        run: |
          if [ "${{ matrix.os-name }}" = "linux" ]; then
            xvfb-run -a npm run test:e2e:packaged
          else
            npm run test:e2e:packaged
          fi
      - name: Attest packaged target
        shell: bash
        env:
          RELEASE_TARGET: ${{ matrix.os-name }}-${{ matrix.arch }}
        run: node scripts/release-attestation.mjs package
      - uses: actions/upload-artifact@v7.0.1
        with:
          name: release-attestation-package-${{ matrix.os-name }}-${{ matrix.arch }}
          path: release-attestations/package-${{ matrix.os-name }}-${{ matrix.arch }}.json
          if-no-files-found: error
      - uses: actions/upload-artifact@v7.0.1
        with:
          name: bear-harness-${{ matrix.os-name }}-${{ matrix.arch }}
          path: ${{ matrix.upload-glob }}
          if-no-files-found: error

  release-gate:
    needs: [quality, upstream-brand, security, recovery, e2e, web-e2e, live-model, package]
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7.0.1
      - uses: actions/setup-node@v7.0.0
        with:
          node-version: 24.19.0
          cache: npm
      - uses: actions/download-artifact@v7.0.1
        with:
          pattern: release-attestation-*
          path: release-attestations
          merge-multiple: true
      - run: node scripts/release-attestation.mjs final
      - uses: actions/upload-artifact@v7.0.1
        with:
          name: release-attestation-final
          path: release-attestations/final.json
          if-no-files-found: error
