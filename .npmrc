# 工具链契约 —— 随仓库提交，不要删除这两个开关：
# - engine-strict=true：npm 安装时强制执行 package.json 的 engines
#   （node 24.19.0 / npm 11.17.0），版本不符直接失败而不是警告。
# - strict-allow-scripts=true：未审阅的 lifecycle script（postinstall 等）
#   直接令安装失败；已逐包审阅并批准的脚本记录在根 package.json 的
#   allowScripts 字段。安全基线：没审过就不执行。
# 本文件不含任何凭据或 registry 覆盖；如需要私有 registry / token，
# 不要写进这个入库文件。
engine-strict=true
strict-allow-scripts=true
